{
  "firestore": {
    "rules": "firebase deploy",
    "indexes": "firestore.indexes.json"
  },
  "hosting": {
    "public": "build/web",
    "predeploy": "flutter build web --release",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"]
  },
  "storage": {
    "rules": "storage.rules"
  }
}
